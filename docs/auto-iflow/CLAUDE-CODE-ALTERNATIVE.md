# 🔄 Альтернатива: Работа через Claude Code

Если Auto-iFlow не работает или вы предпочитаете терминал — используйте **Claude Code** напрямую.

---

## 📥 Установка Claude Code

```bash
# Установить Claude Code CLI
npm install -g @anthropic-ai/claude-code

# Проверить установку
claude --version
```

---

## 🚀 Как работать с Auto-iFlow через Claude Code

### 1. Перейти в проект

```bash
cd /Users/kostenchuksergey/Library/CloudStorage/OneDrive-Личная/VIBE-CODING/Auto-iFlow
```

### 2. Активировать venv

```bash
source .venv/bin/activate
```

### 3. Запустить Claude Code

```bash
claude
```

### 4. Дать задачу

В интерактивном режиме напишите:

```
Прочитай документацию в docs/auto-iflow/ и выполни следующую задачу:
[ваша задача]
```

---

## 📋 Примеры задач

### Запустить stress test:

```
Запусти stress test из spikes/parallel_stress_test.py и покажи результаты
```

### Создать IFlowWrapper:

```
Прочитай спецификацию в .auto-iflow/specs/003-create-iflow-wrapper.md и создай класс IFlowWrapper согласно описанию
```

### Исследовать код Auto-iFlow:

```
Изучи структуру apps/backend/ и найди где происходит вызов Claude CLI. Мне нужно заменить его на iFlow SDK.
```

---

## ⚙️ Полезные команды Claude Code

| Команда | Описание |
|---------|----------|
| `/help` | Показать справку |
| `/clear` | Очистить контекст |
| `/compact` | Сжать историю |
| `Ctrl+C` | Прервать выполнение |
| `exit` | Выйти |

---

## 🔧 Настройка для Auto-iFlow

Создайте файл `.claude/settings.json` в проекте:

```json
{
  "model": "claude-sonnet-4-20250514",
  "permissions": {
    "allow_write": true,
    "allow_execute": true
  },
  "context": [
    "docs/auto-iflow/CONTEXT.md",
    ".auto-iflow/context.md"
  ]
}
```

---

## 📊 Сравнение: Auto-iFlow vs Claude Code

| Функция | Auto-iFlow | Claude Code |
|---------|-------------|-------------|
| Интерфейс | GUI (Electron) | Терминал |
| Параллельные агенты | До 12 | 1 |
| Kanban Board | ✅ | ❌ |
| Git Worktrees | ✅ Автоматически | Вручную |
| Простота | Средняя | Высокая |
| Гибкость | Средняя | Высокая |

---

## 💡 Когда использовать Claude Code?

- Для быстрых одиночных задач
- Если Auto-iFlow не запускается
- Для глубокого исследования кода
- Когда нужен полный контроль

## 💡 Когда использовать Auto-iFlow?

- Для параллельной работы нескольких агентов
- Когда нужен визуальный контроль
- Для управления большими проектами
- Для автоматического merge веток
