# 🖥️ Как работать с Auto-iFlow в Auto-iFlow

## 📋 Быстрый старт

### 1. Открыть проект

1. Запустите **Auto-iFlow** приложение
2. В боковой панели найдите проект **"Auto-iFlow"**
3. Если не видите — нажмите **File → Open Project**
4. Выберите папку: `/VIBE-CODING/Auto-iFlow/`

---

## 🗂️ Структура интерфейса Auto-iFlow

```
┌─────────────────────────────────────────────────────────────┐
│  [Projects] [Tasks] [PRs] [Terminal] [Settings]             │  ← Верхние вкладки
├────────────┬────────────────────────────────────────────────┤
│            │                                                │
│  Список    │   Основная панель:                            │
│  проектов  │   - Kanban Board с задачами                   │
│            │   - Детали выбранной задачи                   │
│            │   - Терминалы агентов                         │
│            │                                                │
└────────────┴────────────────────────────────────────────────┘
```

---

## 📝 Где писать задачи?

### Вариант 1: Через Kanban Board (UI)

1. Перейдите на вкладку **"Tasks"** или **"Kanban"**
2. Нажмите **"+ New Task"** или **"Create Task"**
3. Заполните:
   - **Title:** Название задачи
   - **Description:** Подробное описание
4. Нажмите **Create**

### Вариант 2: Через Specs (файлы)

1. Создайте файл в `.auto-iflow/specs/`
2. Формат: `XXX-task-name.md`
3. Пример уже есть: `001-setup-poc-environment.md`

**Расположение:** `/Auto-iFlow/.auto-iflow/specs/`

---

## 🚀 Как запустить агента?

### Способ 1: Через UI

1. Выберите задачу в Kanban
2. Нажмите **"Start Build"** или **"Run"**
3. Смотрите прогресс в терминале справа

### Способ 2: Через Agent Terminal

1. Откройте вкладку **"Terminal"**
2. Выберите **"Agent Terminal"**
3. Введите задачу текстом

---

## 👀 Где смотреть результаты?

### 1. Терминалы агентов

- Справа показываются терминалы каждого агента
- Видно что агент делает в реальном времени
- До 12 терминалов параллельно

### 2. Изменения в коде

- Агенты работают в отдельных **worktrees**
- После завершения — автоматический merge
- Смотрите в **Git** или **PRs** вкладке

### 3. Файлы проекта

- Открывайте в VS Code или Cursor
- Путь: `/VIBE-CODING/Auto-iFlow/`

---

## 📌 Готовые задачи для запуска

Уже созданы 3 спецификации:

| Файл | Задача |
|------|--------|
| `001-setup-poc-environment.md` | Настроить venv, установить SDK |
| `002-run-stress-test.md` | Запустить 12 агентов параллельно |
| `003-create-iflow-wrapper.md` | Создать IFlowWrapper класс |

**Как использовать:**
1. Откройте файл спецификации
2. Создайте Task на основе описания
3. Запустите агента

---

## ⚠️ Важные моменты

1. **Ветка:** Убедитесь что активна `feature/auto-iflow-integration`
2. **venv:** Уже создан в `.venv/`
3. **iFlow CLI:** Установлен глобально (v0.4.14)
4. **SDK:** Установлен в venv (v0.2.0)

---

## 🛠️ Если что-то не работает

1. Проверьте что iFlow CLI доступен: `iflow --version`
2. Активируйте venv: `source .venv/bin/activate`
3. Перезапустите Auto-iFlow
4. Смотрите логи в `~/Library/Logs/auto-iflow-ui/`
